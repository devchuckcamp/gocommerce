# Migrations + seeding tools (migrations/examples/postgresql)
# Copy to .env (gitignored) if you use a dotenv loader, or export vars in your shell.
# These Go tools read env vars directly; they do NOT auto-load .env by themselves.

DB_DSN=postgres://commerce:commerce@localhost:5432/commerce?sslmode=disable

# Optional parts form
DB_DRIVER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=commerce
DB_PASSWORD=commerce
DB_NAME=commerce
